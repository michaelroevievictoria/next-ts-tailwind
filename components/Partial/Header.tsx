import Head from 'next/head';

const Header = () => {
    return (
        <Head>
            <title>AidBuilder</title>
            <meta name="viewport" content="initial-scale=1.0, width=device-width" />
        </Head>

    )
}

export default Header